{
  "name": "node-css-image",
  "author": "mcoop320",
  "version": "0.0.1",
  "description": "Apply css filters to an image in nodejs and save.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^8.0.0"
  }
}